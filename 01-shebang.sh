# 1 bin/bash

# start of the line in bash scripting should always use a shebang notation

# Apart from the first line none of the lines considered as shebang

# Shebang notation should always start with #1

# SHEBANG : Using this notation we are going to tell system that the interpreter to  be used for this script is bash or python or shell

echo hello world
echo This is the first session of the Bashgi
echo So we know how to run a script